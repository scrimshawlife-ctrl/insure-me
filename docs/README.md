# Docs

`testing/` holds runner notes for the synthetic core.

`site/` is the GitHub Pages board for the two-door operator model. It is
static HTML. It is not the live quote app.

Published path after Pages is enabled:

https://scrimshawlife-ctrl.github.io/insure-me/

Set repository Pages source to **GitHub Actions**. Do not add a custom
domain.
